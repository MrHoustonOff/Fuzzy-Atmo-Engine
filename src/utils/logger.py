# Файл: src/utils/logger.py
"""
Модуль для централизованной настройки и инициализации логгера.

Использование единого объекта 'console' из библиотеки 'rich'
позволяет обеспечить консистентный и стилизованный вывод
во всем приложении.
"""
from rich.console import Console

# Глобальный экземпляр консоли для всего проекта.
# Импортируется в другие модули для вывода информации.
# highlight=True: автоматически подсвечивает синтаксис (например, в JSON).
# color_system="auto": автоматически определяет поддержку цветов в терминале.
console = Console(highlight=True, color_system="auto")